import { ReactElement } from "react";
import { View } from "react-native";
import styles from "./styles";

const Search = (): ReactElement => {
  return <View style={styles.container}>Search</View>;
};

export default Search;
